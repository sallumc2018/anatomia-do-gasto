# Dependências diretas do projeto
pdfplumber==0.11.9
pandas==3.0.2
PyMuPDF==1.26.7

# Necessário para baixar PDFs automaticamente (extrator_saude.py)
requests==2.33.1
beautifulsoup4==4.14.3

# Para geração futura de XLSX
openpyxl==3.1.5

# Retry automático em requisições HTTP instáveis (FNS, transferências, RREO, Transferegov)
tenacity==9.1.4

# Scraping de portais com WAF/403 (9 scripts de coleta)
playwright==1.59.0

# Cache semântico persistente de consultas de IA (economia de tokens)
gptcache==0.1.43

# Integração Cloudflare R2
boto3==1.34.115

# Validação e consolidação de dados orçamentários
duckdb==1.5.3

# Parsing XML resistente a entidades maliciosas (baixar_fns_repasses.py)
defusedxml==0.7.1

# Execução da suíte de testes (24 arquivos em pipelines/testes, tools/gates,
# tools/security e tools/server). Não estava declarado, e por isso não existia
# em nenhum ambiente: nem no venv da VPS, nem no CI. As suítes nunca rodaram
# até 15/08/2026 — quando rodaram, 49 testes passaram de primeira.
pytest==9.1.1
