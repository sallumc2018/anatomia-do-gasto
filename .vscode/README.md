# VS Code local configuration

Este diretório contém recomendações de extensões e configurações padrão para este projeto.

## Extensões recomendadas

Veja `extensions.json` para a lista completa.

## Configurações

- `settings.json`: formato automático com Prettier, Python linting, GitLens ativado

Não adicione dados sensíveis aqui.
